# @atlas-art/ui-core

Platform-agnostic UI core.

Includes:
- typed style recipes (`buttonVariants`, `inputVariants`, ...)
- utility helpers (`cn`)
- fallback design tokens stylesheet (`styles/fallback.css`)
